* Example code
Shaders:
- =texture.frag=: Simple shader that loads a texture and moves it around the screen.
- =raymarch.frag=: Simple raymarcher fragment shader.
  

Scheme code:
- =myscheme.scm=: Some sample Scheme code I have used with the examples. 
